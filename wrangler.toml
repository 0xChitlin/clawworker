name = "clawworker"
main = "src/index.ts"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# ── Public vars ───────────────────────────────────────────────────────────────
# Secrets go in .dev.vars (local) or `wrangler secret put <KEY>` (production)
[vars]
AGENT_ID = "default"
GCP_HOST = "35.188.22.105"
AGENT_PORT = "80"

# ── Secrets (set via: wrangler secret put GATEWAY_TOKEN) ─────────────────────
# GATEWAY_TOKEN
# ADMIN_TOKEN
